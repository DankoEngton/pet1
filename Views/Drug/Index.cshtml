@model IEnumerable<WebApplication2.Models.Drug>
 
<h2>Список препаратів</h2>
<p><a asp-action="Create">Add drug</a></p>
<table class="table">
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Category.Name</td>
        </tr>
    }
</table>
